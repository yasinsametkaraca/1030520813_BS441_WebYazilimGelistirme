import React from 'react';

const Result = (props) => {

    return (
        <div>
            <p>Game Result : <strong>{props.resultMatch}</strong></p>
        </div>
    );
};

export default Result;
